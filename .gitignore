# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp

# Env
.env
.env.local
*.env
# Convention: local secrets for deploy scripts go here (root .env.local).
# Auto-loaded by deploy/release.sh.

# Server (Go)
server/bin/
server/dist/
server/tmp/
server/coverage.out

# Client (Node / Electron)
client/node_modules/
client/dist/
client/dist-electron/
client/out/
client/release/
client/.vite/

# Logs
*.log
logs/

# Data volumes
data/
deploy/data/
deploy/.env.prod

# Operator-only — contains production server credentials. Never commit.
server.rtf
server.txt
server.md
server*.rtf
*.credentials

# Local build caches
.cache/

# Built binaries (release artifacts belong in GitHub Releases, not the repo)
*.dmg
*.exe
*.AppImage
*.zip

# Internal brand-asset working folder (regenerated via OpenAI images-2)
DFimages/
.claude/
